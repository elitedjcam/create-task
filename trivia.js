let bbScore;
let bkScore;
let fbScore;
let tnScore;

function baseballScore {
  if /*The correct answer is clicked*/ {
    bbScore++
  } else /*The wrong answer is clicked*/ {
    bbScore = bbScore
  }
}
